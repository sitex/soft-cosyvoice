# Structure

- Root: examples, `README.md`, `PROJECT_GOAL.md`, `requirements.txt`, `cosyvoice/`, `runtime/`, `examples/`, `tools/`, `tests/`.
- Package: `cli`, `dataset`, `bin`, and supporting model/frontend modules.
- Existing untracked `probe_instruct_format.py`, `src/`, and `tests/` were pre-existing and were not modified.
- No `scripts/verify` exists.
